/target
target/
/vendor-artifacts/
/work/locks/
/work/worktrees/
/work/diffs/
/logs/
node_modules/
.logos-native/
# Stryker mutation scaffold (W2.5): sandbox scratch + per-run reports are ephemeral. The tracked
# artifacts are stryker.conf.json + conformance/mutation-floor.json (the recorded score) only.
.stryker-tmp/
reports/
*.log
.ratchet-break
.merge-freeze
# ledger prior-state snapshots are a working-tree/fixture seam only — a committed one forges the
# monotonicity baseline (ledger-lint bans them at HEAD too). SCHEMA §4 step 1 / §10.
*.tsv.head
