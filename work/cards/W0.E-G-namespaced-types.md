# W0.E-G — logicaffeine G-task: namespaced cross-module type references (`Alias::Type`)

repo: logicaffeine — ITS CLAUDE.md absolute (TDD, never git, one suite at a time).
Found by: W0.E multimodule canary, first-ever end-to-end multi-module largo build.
Blocks: Wave-0 exit (gate --wave 0).

## The gap
`discover_with_imports` + `merge_registry` (crates/logicaffeine_compile/src/analysis/
discovery.rs) intern imported types as `"{Alias}::{Type}"` (e.g. `Geometry::Point`), but
the surface language cannot utter them: the lexer has no `::` handling (`:` is bare
punctuation, lexer.rs:1208), so `Let p be a new Geometry::Point with x 3 and y 4.` dies
with ParseError { ExpectedStatement } at the `::`. Zero existing tests use `new X::Y` —
the feature's missing half. Constructor parse sites: parser/mod.rs:7560+.

## Repro (the toy the fix must make pass)
main.lg abstract imports `[Geometry](file:./geometry.md)` + `[Colors](file:./colors.md)`;
modules define `## A Point has:` / `## A Color has:` structs; Main constructs
`a new Geometry::Point with x 3 and y 4`, reads fields possessively, Shows the sum → 263.

## Direction (validate, then do the natural thing — lift and shift left)
Hypothesis: lex `Ident::Ident` (no spaces) as ONE Word token whose text matches the
interned registry name exactly — then construction, type annotations, and generics work
wherever a type-name token flows. Watch: time-literal `:` guard (lexer.rs:1054); codegen
must sanitize `::` in generated Rust identifiers (e.g. `Geometry__Point`) across ALL
tiers that name structs (AOT codegen at minimum — the canary builds via largo AOT);
typechecker lookup must hit the interned name. Do NOT change discovery's interning.

## RED first
New test file in crates/logicaffeine_tests: end-to-end multi-FILE program (tempdir +
loader/discover_with_imports or the same entry path largo build uses) asserting the toy
above prints 263 — plus a type-annotation usage (`Let q: Geometry::Point be …` or the
house annotation form) and a negative (unknown alias `Bogus::Point` errors cleanly).

## Exit
RED → GREEN; targeted `cargo nextest run` for the new file + neighbors; full
`./scripts/run-all-tests-fast.sh` ONLY if no other suite is running on the box
(check `pgrep -f 'nextest|cargo test'` first; if busy, report and stop).
Report changed files + design notes for the adversarial review round. DO NOT commit.
