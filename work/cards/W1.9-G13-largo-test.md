# W1.9 — G13: `largo test` + the LOGOS test framework (tests-in-LOGOS keystone)

repo: logicaffeine  ← WORK IN /home/tristen/logicaffeine UNDER ITS CLAUDE.md.
NEVER run git there. TDD absolute. The stub is RESERVED for exactly this:
`apps/logicaffeine_cli/src/cli.rs:622` (Commands::Test) errors with
"`largo test` is reserved for the LOGOS test framework (coming in a future release)".

## Design substrate (already in the language — build ON it, don't reinvent)
`token.rs:216–229`: `## Test` sections; `Expect that <cond>.` (test-only,
records-and-continues, refused outside ## Test/## Example); `Require that <cond>.`
(hard assert); `Advance time by <dur>.` (virtual clock). Find the parser/interpreter
handling of these tokens and the ## Test section semantics before designing anything.

## Deliverable
1. `TESTS_IN_LOGOS.md` design doc (in logicaffeine work/ per house convention) — WRITTEN
   FIRST and adversarially reviewed (2 diff-only reviewers) before implementation (§2.5
   plan-docs-first). Contents: test discovery (`tests/*.lg` files in a largo project +
   `## Test` sections in any module), execution model (each ## Test body runs isolated;
   Expect failures recorded and continue; Require aborts the test), reporting format
   (per-test PASS/FAIL lines + summary + nonzero exit on any failure + `--json` for
   machine consumption — logos-bun's ledger tooling will parse it), assertion-count
   surfacing (logos-bun needs executed-Expect counts for its anti-skip parity ledger, L5).
2. `largo test` implementation in apps/logicaffeine_cli (replace the stub):
   discovery → build test targets (reuse the existing build pipeline) → run → report.
   Tier: run on the default execution path (tree-walker/VM as largo run does today).
3. At least: `largo test` (all), `largo test <file>` (one), `--json`.

## RED battery (FIRST)
- CLI-level tests in the house CLI-test location: a toy project with 2 passing + 1 failing
  `## Test` → exit nonzero, report names both failures (Expect continues, Require aborts);
  all-green project → exit 0; `--json` output shape locked by a golden.
- An `Expect that` outside a ## Test body still refuses (existing semantics must not
  loosen — find and cite the existing test that locks this; extend, never weaken).

## Exit
Targeted suite + `./scripts/run-all-tests-fast.sh` green. Report: exact CLI surface +
JSON schema so logos-bun's gate.sh can consume `largo test --json` at W2.9.
DO NOT commit (user commits; pin bump at wave boundary).
