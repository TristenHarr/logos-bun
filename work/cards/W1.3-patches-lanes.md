# W1.3 — P0.3 patch series + scratch-worktree materializer + Lane-A validity lint

repo: logos-bun

## Deliverable
- `conformance/patches/0001-bunexe-override.patch` — the 3-line change to
  `test/harness.ts` `bunExe()` (line ~120 at pin; VERIFY content at bun-v1.3.14 — it has
  NO env override today) honoring `BUN_EXE_OVERRIDE`. Content-anchored (context lines),
  not line-number-anchored.
- `conformance/patches/0002-assert-counter.patch` — instruments the expect() counter to
  dump executed-assertion count at process exit (feeds W1.2's asserts column).
- `scripts/worktree.mjs` — materializes a scratch worktree of vendor/bun under
  `work/worktrees/<id>/` (git worktree from the submodule — read-only on vendor/bun
  itself), applies the patch series, returns the path; `--clean` removes. Patch-apply
  failure = loud nonzero exit (the re-baseline tripwire).
- `conformance/lint-lanes.mjs` — scans a test file for in-process API usage
  (`Bun.build(`, `Bun.serve(`, `bun:ffi`, direct `import` of bun-internal modules) and
  auto-marks its row `BLOCKED(P9)`; a Lane-A PASS for such a file is a lint failure.

## RED battery (FIRST)
- patched worktree: `bunExe()` returns `process.env.BUN_EXE_OVERRIDE` when set (run a tiny
  node script against the patched harness.ts via the worktree).
- corrupt patch fixture → worktree.mjs exits loud.
- `expectBundled.ts:147` still honors `BUN_EXE` at the pin (assert the line content —
  the pin-bump canary; zero patching needed for bundler tests).
- lint fixture: a toy test containing `Bun.build(` → BLOCKED(P9); a spawn-only test → clean.
- vendor/bun stays pristine after materialize+clean (git status --porcelain empty).

## Ratchet at exit
L4 (lane lint over Lane-A rows) wired into gate.sh; L7 already covers worktree hygiene.

## Manifest
conformance/patches/**, scripts/worktree.mjs, conformance/lint-lanes.mjs, red/p0/lanes/**,
gate.sh (L4), tests-shim-allowlist.tsv (append).
