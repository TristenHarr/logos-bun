# W1.2 — P0.2 conformance/runner.mjs fork + assertion-count capture

repo: logos-bun

## Deliverable
Fork `vendor/bun/scripts/runner.node.mjs` → `conformance/runner.mjs` (runs under node;
KEEP its `--exec-path` plumbing — line 103/484 at the pin) adding:
- `--oracle-path <bin>` (oracle-bun), `--lane A|B|C`, `--ledger <tsv>` (emit result rows
  in W1.1's SCHEMA.md format, through a queue file promote.mjs consumes — the runner NEVER
  writes ledgers directly).
- per-file junit XML.
- per-file EXECUTED-ASSERTION COUNTS captured into the row's `asserts` column (works with
  W1.3's 0002-assert-counter.patch; the counter dumps at process exit).

## RED battery (FIRST)
`red/p0/runner/` toy suite (3 files: pass / fail / skip) run through runner.mjs:
- pass/fail/skip statuses land correctly in the emitted rows
- junit XML golden matches
- the skip file yields asserts=0 → visible delta vs its sibling (the anti-skip signal, L5)
- `--lane A` rows carry LANE=A

## Oracle
Toy-suite goldens (self-oracle).

## Ratchet at exit
L5 assert-parity check lands in gate.sh: a PASS row whose recorded asserts drops below its
promotion-time value fails the gate.

## Manifest
conformance/runner.mjs, red/p0/runner/**, gate.sh (L5 wiring), tests-shim-allowlist.tsv
(append; replaced-by W2.9 where expressible — note runner.mjs itself is PERMANENT node,
CLAUDE.md R3 carve-out: it hosts bun's own TS suite).
