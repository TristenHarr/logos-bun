# W1.7 — GIFT.2 upstream-gifts ledger + templates

repo: logos-bun

## Deliverable (BAKE_A_BUN §9.4 is the spec — all 20 invariants)
- `conformance/upstream-gifts.md` — the finding state machine:
  `found → classified → {embargoed | ready} → filed → {in-review | changes-requested} →
  {merged | declined | duplicate | superseded-upstream | stale} → re-baselined`,
  one row per finding + a lint (`scripts/lints/gifts-lint.mjs`) rejecting illegal
  transitions and missing classification fields (ours/theirs/spec-ambiguity;
  security y/n — security=y rows may NEVER carry a public artifact link, invariant 10).
- Templates under `conformance/templates/`:
  - `gift-pr-body.md` — bun's PR template shape (What does this PR do? / How did you
    verify?) + mandatory provenance & AI-authorship disclosure block (invariants 12–13).
  - `security-routing.md` — the security@bun.com checklist (SECURITY.md flow; never a
    public PR/issue first).
  - `duplicate-search.md` — pre-filing search checklist (invariant 11).

## RED battery (FIRST)
- gifts-lint fixture: illegal transition (`found → filed`, skipping classified) → reject.
- security=y row with a PR URL → reject.
- valid full-lifecycle fixture → accept.

## Ratchet at exit
upstream-gifts.md joins the W1.1 hash-chain mechanism (state transitions are chained,
append-only history).

## Manifest
conformance/upstream-gifts.md, conformance/templates/**, scripts/lints/gifts-lint.mjs,
red/p0/gifts/**, tests-shim-allowlist.tsv (append).
