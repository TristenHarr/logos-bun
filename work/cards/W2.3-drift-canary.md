# W2.3 — P0.9 drift-canary vs upstream bun HEAD + re-baseline ritual

repo: logos-bun. Non-blocking lane (§6.3): shows where upstream is moving before a pin bump.

## Deliverable
- `conformance/drift-canary.mjs`: compares the test-file set at the current SPEC_PIN
  (bun-v1.3.14, in vendor/bun) against a "newer upstream" set — files added since the pin.
  Since we can't fetch live upstream in this env, drive it against a FIXTURE representing
  "upstream HEAD added these files" (a list) and report which new test files exist that our
  ledger doesn't cover yet → `conformance/ledger/drift.tsv` (separate, NEVER gates merges).
- The re-baseline ritual is already documented in SPEC_PIN.md — verify it's complete and
  add any missing step (bump pin → re-apply patch series loud-on-fail → frontier-scan new
  files → triage into phases → refetch+sha the oracle binary → incident record).
- A `--check` mode reporting drift count; wired as a NON-BLOCKING gate note (prints, never reds).

## RED battery
`red/p0/drift/`: a fixture "upstream added foo.test.ts, bar.test.ts" not in our ledger →
canary detects exactly those 2 as drift; a fixture where all upstream files are covered →
zero drift. Deterministic.

## Ratchet at exit
drift.tsv exists; the canary is documented as non-gating (a drift row is informational).

## Manifest
conformance/drift-canary.mjs, conformance/ledger/drift.tsv, red/p0/drift/**, SPEC_PIN.md
(ritual completeness only), conformance/tests-shim-allowlist.tsv. Read-only git. No commit.
