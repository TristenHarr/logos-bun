# W1.8 — G2-early: subprocess spawn/capture + sha256 digest (tests-in-LOGOS enabler)

repo: logicaffeine  ← WORK IN /home/tristen/logicaffeine UNDER ITS CLAUDE.md.
NEVER run git there. TDD: RED test first, run-all-tests-fast.sh green before card close.

## Why now (pulled forward from P3/G2)
CLAUDE.md R3 (logos-bun): tests are written in LOGOS. Spawning oracle-bun/logos-bun from a
`.lg` test needs subprocess support; verifying the oracle binary needs sha256. These are
the two capability gaps blocking the W2.9 shim→.lg migration.

## Deliverable (house pattern: native kernel + std surface + tier lock, crypto.lg template)
1. `## To native` kernels in `crates/logicaffeine_compile/src/semantics/builtins.rs`
   registry (impls in `logicaffeine_system`), following the existing io/file/env pattern:
   - `run (cmd: Text, argv: Seq of Text) -> CommandResult` — spawn, wait, capture
     stdout/stderr/exit code. CommandResult = struct {out: Text, err: Text, code: Int}
     (exact shape: follow existing Option/struct-return precedent in std surfaces; if raw
     bytes vs Text is contentious, Text-lossy now + bytes variant documented as follow-up).
   - environment/cwd variants deferred to full G2 (P3) — KEEP THIS MINIMAL: spawn+capture
     is what .lg tests need.
   - `sha256File (path: Text) -> Text` — hex digest, streaming (oracle binary is ~90MB).
2. `assets/std` surface: new `process.lg` (and sha256File into the right existing module —
   `file.lg` or `crypto.lg`; pick by house precedent) in BOTH assets/std locations if the
   repo keeps mirrored copies (check how existing modules are mirrored: root assets/std vs
   crates/logicaffeine_compile/assets/std).
3. Demand-import wiring exactly like env/file/time (loader prelude tables).

## RED battery (FIRST — in crates/logicaffeine_tests/tests/, house naming)
- spawn `/bin/echo hi` → out "hi\n", code 0; spawn `false` → code 1; spawn missing binary
  → error path (house error convention, NOT a panic).
- tw == VM == AOT differential on a spawn-using program (the house 3-tier lock).
- sha256File KAT: known file → known digest (RFC test vector via a fixture file).
- Determinism note: spawn is IO — check how existing IO natives are classified for the
  PE/purity oracles (`effects` tables) and register identically. A misclassified effect
  breaks Futamura locks — this is load-bearing.

## Exit
Targeted suite green + `./scripts/run-all-tests-fast.sh` green (one suite at a time!).
Report back: exact std surface names/signatures for the W2.9 migration to consume.
DO NOT commit (user commits logicaffeine; pin bump happens at the wave boundary).
