# W2.5 — P0.11 mutation-testing scaffold (Stryker now; cargo-mutants → W4)

repo: logos-bun. Eat our own dog food: a harness that can't catch mutants can't catch
regressions (§8). The harness's OWN mutation score is a tracked metric.

## Deliverable
- **Stryker (JS mutation)** over the node harness surface NOW: runner.mjs, the comparators
  (diffcli/normalize/treehash), the lints (ledger-lint, lint-lanes, assert-parity, gifts-lint,
  workflow-ops-lint), fuzz-driver, bench/lib. Config `stryker.conf.json` (or `.mjs`); the
  test command = the relevant red/p0 drivers. If Stryker isn't installed, `npm i -D
  @stryker-mutator/core` locally (note it in the report; package.json/package-lock are new
  files — a scoped, declared dep, fine).
- A **mutation-score floor** ratchet: `conformance/mutation-floor.json` (per-target killed/total
  floor, only rises). Gate check L14 (mutation) — but GUARD it: full Stryker runs are slow, so
  L14 in the gate should verify the LAST RECORDED score meets the floor (a cheap read of a
  committed score file), NOT run Stryker inline. A `--run` mode does the actual (slow) Stryker
  pass and updates the score file; the gate just checks floor compliance. Wire L14 (floor check)
  into gate.sh house-style with an empty-guard.
- **cargo-mutants** config STAGED for W4 (when Rust native kernels + oracle shims exist): a
  `conformance/oracle/shims/mutants.toml` stub OR a note in gate-manifest (W1.6's guard already
  expects `conformance/oracle/shims/mutants.toml` when shims/ appears) — coordinate: create the
  stub so the guard is satisfiable, or document that it lands at W4.

## RED battery
`red/p0/mutation/`: a PLANTED mutant in one comparator (e.g. flip a `===` to `!==` in a copy)
must be KILLED by the existing red/p0 tests (survives → the test suite is too weak → gate should
flag). Prove the loop: a planted surviving mutant drops the score below floor → L14 reds.

## Ratchet at exit
L14 (floor read) in gate.sh; mutation-floor.json committed with the first real Stryker score.

## Manifest
stryker.conf.*, conformance/mutation-floor.json, red/p0/mutation/**, scripts/gate.sh (l14),
package.json + package-lock.json (Stryker dep), conformance/tests-shim-allowlist.tsv,
conformance/oracle/shims/mutants.toml (stub for W1.6's guard, or documented). Read-only git.
gate.sh hot-file: add ONLY l14 + append to the sequence.
