# W1.6 — gate.sh --wave mode + gate-manifest (L9) + THE GATE-AUDIT meta-lock

repo: logos-bun. The meta-lock: proves the gate CATCHES every planted violation. This wave
(Wave 1) proved how many real bugs hide behind green-in-isolation — so this is load-bearing.

## Deliverable
1. `scripts/gate.sh` `--wave N` mode: `--full` + wave-N exit criteria (per WAVES.md). Keep the
   existing sequence (l15 l6 l7 l8 l_freeze l1 l4 l5 l17 l16_seed) intact.
2. `scripts/gate-manifest.json` (L9 — generalized ratchet-before-code): maps guarded
   artifacts → required guards. Seed: `bun-engine/` → seam grep-lock (W8); `fuzz/*/` →
   regression bank in replay set (W2.1); `bench/LEDGER.json` locks → 3σ verify (W2.2);
   `conformance/oracle/shims/` → cargo-mutants config (W4). A guarded path existing WITHOUT
   its registered guard = gate failure. Add `l9()` to the sequence.
3. `red/p0/gate-audit/` — THE META-LOCK. One planted violation per enforcement rule, each a
   fixture the driver asserts gate.sh CATCHES (reds on). Cover EVERY live rule:
   - hand-edited PASS (chain broken) · shrunk PASS set vs HEAD · expired QUARANTINE ·
     in-process-test-as-Lane-A-PASS (L4) · dirty vendor (L7) · pin mismatch (L6) ·
     forbidden git verb in a script (L8) · out-of-manifest commit (commit.mjs) ·
     impl-before-RED (commit.mjs L10) · committed .merge-freeze/.ratchet-break/.tsv.head ·
     unallowlisted .mjs shim (L16) · lost CLAUDE.md anchor (L15) · **off-tag lint crash
     reds the gate (the B1 regression guard — l1/l4/l5 must fail on nonzero exit)** ·
     assert-parity drop (L5) · illegal gift transition (L17) · security-public-link (L17) ·
     guarded-dir-without-guard (L9).
4. `VIOLATIONS_CAUGHT` floor constant (futamura_ratchet.rs pattern): only rises. The audit
   re-runs at EVERY wave exit forever — a gate that stops catching a plant is a regression.

## RED battery
The gate-audit IS the battery: the driver fails while ANY plant goes uncaught. Write it, run
it, watch it fail against the current gate (find a rule the gate does NOT yet catch → that's a
real gap to close), then make every plant caught.

## Ratchet at exit
gate-audit wired into `--wave N`; VIOLATIONS_CAUGHT floor committed. Append driver to allowlist.

## Manifest
scripts/gate.sh, scripts/gate-manifest.json, red/p0/gate-audit/**, conformance/tests-shim-allowlist.tsv.
Do NOT modify other cards' committed lints — the audit DRIVES them, doesn't change them.
