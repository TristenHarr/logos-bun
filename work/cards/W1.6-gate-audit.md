# W1.6 — gate.sh v1 + gate-manifest + THE GATE-AUDIT (serial; integrates W1.1–W1.5)

repo: logos-bun
serial: runs AFTER W1.1–W1.5, W1.7 are GREEN (it wires their checks together).

## Deliverable
- `scripts/gate.sh` v1: L1–L10 + L15 + L16 assembled; `--quick` (seconds: L1–L4, L6–L10,
  L15, L16) vs `--full` (adds batteries) vs `--wave N` (full + wave-exit criteria).
- `scripts/gate-manifest.json` — the generalized ratchet-before-code lock (L9): maps
  guarded artifacts → required guards. Seed entries: `bun-engine/` → seam grep-lock
  registered; `fuzz/*/` → regression bank in the replay set; `bench/LEDGER.json` locks →
  3σ verify wired; `conformance/oracle/shims/` → cargo-mutants config present. A guarded
  path existing without its guard = gate failure.
- `red/p0/gate-audit/` — THE META-LOCK: one planted violation per enforcement rule
  (hand-edited PASS, shrunk PASS set, expired quarantine, in-process-test-as-LaneA-PASS,
  dirty vendor fixture, pin mismatch fixture, forbidden git verb, out-of-manifest commit,
  impl-before-RED, guarded-dir-without-guard, unallowlisted .mjs test, lost CLAUDE.md
  anchor) with a driver asserting gate.sh catches EVERY one.
  `VIOLATIONS_CAUGHT_FLOOR` constant (futamura_ratchet pattern): only rises.
- `WAVES.md` gate-log appender (gate.sh already stamps; make it append the line).

## RED battery
The gate-audit IS the battery: driver fails while any plant goes uncaught.

## Ratchet at exit
The audit runs at every wave exit forever (`--wave N` includes it). Floor constant
committed. Wave-1 exit ALSO requires the end-to-end Lane-A smoke: a trivial oracle-hosted
test spawns logos-bun via BUN_EXE_OVERRIDE (W1.3 worktree + W1.2 runner), its row enters a
real ledger via promote.mjs, chain valid.

## Manifest
scripts/gate.sh, scripts/gate-manifest.json, red/p0/gate-audit/**, WAVES.md (gate log),
tests-shim-allowlist.tsv (append).
